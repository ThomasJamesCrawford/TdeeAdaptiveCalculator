<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TDEE.Converters"
             x:Class="TDEE.Views.Goal">
    
    <ContentPage.Resources>
        <local:DoubleToStringConverter x:Key="DoubleToStringConverterKey"/>
    </ContentPage.Resources>
    
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding GoalWeight, Converter={StaticResource DoubleToStringConverterKey}}"
                   VerticalOptions="Center"
                   HorizontalOptions="FillAndExpand"/>
            <Entry Text="{Binding GoalRate, Converter={StaticResource DoubleToStringConverterKey}}"
                   VerticalOptions="Center"
                   HorizontalOptions="FillAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>